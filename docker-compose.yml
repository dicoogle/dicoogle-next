version: "3.8"

services:
  # backend:
  #   build:
  #     context: ./dicoogle/
  #     dockerfile: Dockerfile
  #   container_name: dicoogle-backend
  #   restart: unless-stopped
  #   ports:
  #     - "8080:8080"

  frontend:
    build:
      context: ./webapp/
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: "/next/api"
        BASE_PATH: "/next/"
        VITE_BASE_PATH: "/next"
        VITE_FILESYSTEM_API_BASE_URL: "/next/files/filesystem"
        FILESYSTEM_SERVER_PORT: 3333
    container_name: dicoogle-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes: 
      - ./webapp/:/home/:ro
